import React from "react";
import { Channel, useChatContext } from "stream-chat-react";

import { ChannelInner, EditChannel, TeamMessage } from "./";

const CreateChannel = () => {
  return <div>Create Channel</div>;
};

export default CreateChannel;
